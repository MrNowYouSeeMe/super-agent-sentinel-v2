# Legacy reference boundary

The uploaded legacy archive is intentionally not copied into this repository.

Initial inspection found duplicated project trees, embedded `.git` directories, `.venv`, `node_modules`, build/coverage output, and `.env` files. Only behavior and contracts will be reviewed externally. V2 has no runtime dependency on the legacy source.
